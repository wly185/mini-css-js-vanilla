package.json/scripts
run error - have to "start":"node <folder/file>" - have to use node, else windows will use other runtime
nodemon as devdependencies . add D flag "npm i -D <package> - nodemon is a package watcher, no need to keep restarting server, saving will trigger nodemon


index.js
file structure - many html, many css
do not use express.res.sendFile
use express app.use, express.static <static folder, by convention /public>
__dirname undefined - to get directory name in nodejs, const __dirname = path.resolve(path.dirname(''));

git commands

inital upload:
git init
--create github repo, copy the url
git commit . -m '<msg>'
git remote add orgin <url>
git checkout -b <branch>
git push origin <branch>

deleting folder:
git checkout <branch>
git rm -r <folder>
git commit . -m '<msg>'
git push origin <branch>

updating file:
git commit . -m "<msg>"

cloning from remote to local
--within local folder
git init
git clone <url>
git commit . -m "<msg>"
git add remote origin <url>
git fetch origin
git push origin <branch>